import { ScholarProgramInformation } from "..\users\scholar\scholar"
import { AdminUser } from "..\users\admin"

model Chapter {
    id                String                      @id @default(nanoid())
    chapterName       String
    asociatedScholars ScholarProgramInformation[]
    asociatedAdmins   AdminUser[]
}
